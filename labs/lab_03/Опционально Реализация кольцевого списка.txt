class CircularLinkedList:
    """Кольцевой односвязный список."""
    
    def __init__(self):
        self.head = None
    
    def append(self, data):
        """Добавление в кольцевой список."""
        new_node = Node(data)
        
        if not self.head:
            self.head = new_node
            new_node.next = self.head  # Замыкаем кольцо
        else:
            current = self.head
            while current.next != self.head:  # Ищем последний узел
                current = current.next
            current.next = new_node
            new_node.next = self.head  # Замыкаем кольцо
    
    def __repr__(self):
        if not self.head:
            return "CircularLinkedList([])"
        
        nodes = []
        current = self.head
        while True:
            nodes.append(repr(current.data))
            current = current.next
            if current == self.head:
                break
        return "CircularLinkedList([" + ", ".join(nodes) + "])"

# Демонстрация
circular_list = CircularLinkedList()
circular_list.append(1)
circular_list.append(2)
circular_list.append(3)
print(circular_list)  # CircularLinkedList([1, 2, 3])
